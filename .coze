[project]
requires = ["python-3.12"]

[dev]
build = ["bash", "-c", "pip3 install -r requirements.txt"]
run = ["python3", "start_faas.py"]

[deploy]
build = ["bash", "-c", "pip3 install -r requirements.txt"]
run = ["python3", "start_faas.py"]
