[project]
requires = ["python-3.12"]

[dev]
build = ["bash", "-c", "pip3 install -r requirements.txt"]
run = ["bash", "start.sh"]

[deploy]
build = ["bash", "-c", "pip3 install -r requirements.txt"]
run = ["bash", "start.sh"]
